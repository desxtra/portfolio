---
import '../styles/global.css'
---

<section
  id="certs"
  class="min-h-screen flex flex-col items-center justify-center bg-white dark:bg-gray-900 text-center px-4 py-16 overflow-visible"
>
  <h2 class="text-4xl font-bold mb-6">Certifications</h2>
  <p class="text-gray-600 dark:text-gray-400 mb-10 max-w-xl">
    Here are some certifications I've earned to strengthen my knowledge and
    credibility in the tech field.
  </p>

  <!-- Horizontal scroll container with arrows -->
  <div class="w-full max-w-5xl overflow-visible relative">
    <!-- Left arrow -->
    <button
      id="scrollLeft"
      class="absolute -left-8 md:-left-16 top-1/2 -translate-y-1/2 -translate-y-10 z-30 border border-black dark:border-white bg-white dark:bg-gray-900 shadow-lg rounded-full p-2 md:p-3 hover:shadow-xl transition-all opacity-0 pointer-events-none"
    >
      <svg
        class="w-4 h-4 md:w-5 md:h-5 text-gray-600 dark:text-gray-400"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M15 19l-7-7 7-7"
        ></path>
      </svg>
    </button>

    <!-- Right arrow -->
    <button
      id="scrollRight"
      class="absolute -right-8 md:-right-16 top-1/2 -translate-y-1/2 -translate-y-10 z-30 bg-white border border-black dark:border-white dark:bg-gray-900 shadow-lg rounded-full p-2 md:p-3 hover:shadow-xl transition-all"
    >
      <svg
        class="w-4 h-4 md:w-5 md:h-5 text-gray-600 dark:text-gray-400"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M9 5l7 7-7 7"
        ></path>
      </svg>
    </button>

    <div id="scrollContainer" class="overflow-hidden pb-20">
      <div
        class="flex gap-4 w-max px-4 transition-transform duration-500 ease-out"
        id="scrollTrack"
      >
        <!-- cert items -->
        <div class="relative group overflow-visible w-72 flex-shrink-0">
          <img
            src="/img/project-hollow.png"
            alt="Certified Cloud Practitioner Certificate"
            class="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 group-hover:-translate-y-4 transition-all duration-500 ease-out rounded-lg shadow-lg w-auto z-10 pointer-events-none"
          />

          <!-- text container -->
          <div
            class="border border-black dark:border-white p-6 rounded-lg shadow hover:shadow-lg transition-transform duration-300 transform group-hover:translate-y-20 bg-white dark:bg-gray-900 relative z-20 h-32 flex flex-col justify-center"
          >
            <a
              href="#"
              target="_blank"
              rel="noopener noreferrer"
              class="block text-xl font-semibold text-black dark:text-white hover:underline mb-2"
            >
              Certified Cloud Practitioner
            </a>
            <p class="text-sm text-gray-500 dark:text-gray-400">
              DewaCloud Academy · Jul 2025
            </p>
          </div>
        </div>

        <!-- cert items -->
        <div class="relative group overflow-visible w-72 flex-shrink-0">
          <img
            src="/img/project-hollow.png"
            alt="Certified Cloud Practitioner Certificate"
            class="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 group-hover:-translate-y-4 transition-all duration-500 ease-out rounded-lg shadow-lg w-auto z-10 pointer-events-none"
          />

          <!-- text container -->
          <div
            class="border border-black dark:border-white p-6 rounded-lg shadow hover:shadow-lg transition-transform duration-300 transform group-hover:translate-y-20 bg-white dark:bg-gray-900 relative z-20 h-32 flex flex-col justify-center"
          >
            <a
              href="#"
              target="_blank"
              rel="noopener noreferrer"
              class="block text-xl font-semibold text-black dark:text-white hover:underline mb-2"
            >
              AWS Solutions Architect
            </a>
            <p class="text-sm text-gray-500 dark:text-gray-400">
              Amazon Web Services · Jun 2025
            </p>
          </div>
        </div>

        <!-- cert items -->
        <div class="relative group overflow-visible w-72 flex-shrink-0">
          <img
            src="/img/project-hollow.png"
            alt="Google Cloud Professional Certificate"
            class="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 group-hover:-translate-y-4 transition-all duration-500 ease-out rounded-lg shadow-lg w-auto z-10 pointer-events-none"
          />

          <!-- text container -->
          <div
            class="border border-black dark:border-white p-6 rounded-lg shadow hover:shadow-lg transition-transform duration-300 transform group-hover:translate-y-20 bg-white dark:bg-gray-900 relative z-20 h-32 flex flex-col justify-center"
          >
            <a
              href="#"
              target="_blank"
              rel="noopener noreferrer"
              class="block text-xl font-semibold text-black dark:text-white hover:underline mb-2"
            >
              Google Cloud Professional
            </a>
            <p class="text-sm text-gray-500 dark:text-gray-400">
              Google Cloud · May 2025
            </p>
          </div>
        </div>

        <!-- cert items (item ke-4 untuk demo scroll) -->
        <div class="relative group overflow-visible w-72 flex-shrink-0">
          <img
            src="/img/project-hollow.png"
            alt="Microsoft Azure Certificate"
            class="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 group-hover:-translate-y-4 transition-all duration-500 ease-out rounded-lg shadow-lg w-auto z-10 pointer-events-none"
          />

          <!-- text container -->
          <div
            class="border border-black dark:border-white p-6 rounded-lg shadow hover:shadow-lg transition-transform duration-300 transform group-hover:translate-y-20 bg-white dark:bg-gray-900 relative z-20 h-32 flex flex-col justify-center"
          >
            <a
              href="#"
              target="_blank"
              rel="noopener noreferrer"
              class="block text-xl font-semibold text-black dark:text-white hover:underline mb-2"
            >
              Microsoft Azure Fundamentals
            </a>
            <p class="text-sm text-gray-500 dark:text-gray-400">
              Microsoft · Apr 2025
            </p>
          </div>
        </div>

        <!-- cert items (item ke-5 untuk demo scroll) -->
        <div class="relative group overflow-visible w-72 flex-shrink-0">
          <img
            src="/img/project-hollow.png"
            alt="Red Hat Certificate"
            class="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 group-hover:-translate-y-4 transition-all duration-500 ease-out rounded-lg shadow-lg w-auto z-10 pointer-events-none"
          />

          <!-- text container -->
          <div
            class="border border-black dark:border-white p-6 rounded-lg shadow hover:shadow-lg transition-transform duration-300 transform group-hover:translate-y-20 bg-white dark:bg-gray-900 relative z-20 h-32 flex flex-col justify-center"
          >
            <a
              href="#"
              target="_blank"
              rel="noopener noreferrer"
              class="block text-xl font-semibold text-black dark:text-white hover:underline mb-2"
            >
              Red Hat Certified Engineer
            </a>
            <p class="text-sm text-gray-500 dark:text-gray-400">
              Red Hat · Mar 2025
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const scrollContainer = document.getElementById("scrollContainer");
    const scrollTrack = document.getElementById("scrollTrack");
    const scrollLeft = document.getElementById("scrollLeft");
    const scrollRight = document.getElementById("scrollRight");

    let currentIndex = 0;
    const itemWidth = 288; // w-72 = 288px
    const gap = 16; // gap-4 = 16px
    const scrollAmount = itemWidth + gap;
    const totalItems = scrollTrack.children.length;
    const maxIndex = totalItems - 3; // Show 3 items at once

    // Update scroll position with smooth animation
    function updateScrollPosition() {
      const translateX = -(currentIndex * scrollAmount);
      scrollTrack.style.transform = `translateX(${translateX}px)`;
      updateArrows();
    }

    // Check scroll position and show/hide arrows
    function updateArrows() {
      scrollLeft.style.opacity = currentIndex <= 0 ? "0" : "1";
      scrollLeft.style.pointerEvents = currentIndex <= 0 ? "none" : "auto";

      scrollRight.style.opacity = currentIndex >= maxIndex ? "0" : "1";
      scrollRight.style.pointerEvents =
        currentIndex >= maxIndex ? "none" : "auto";
    }

    // Scroll functions
    scrollLeft.addEventListener("click", () => {
      if (currentIndex > 0) {
        currentIndex--;
        updateScrollPosition();
      }
    });

    scrollRight.addEventListener("click", () => {
      if (currentIndex < maxIndex) {
        currentIndex++;
        updateScrollPosition();
      }
    });

    // Disable manual scrolling
    scrollContainer.addEventListener("wheel", (e) => {
      e.preventDefault();
    });

    scrollContainer.addEventListener("touchmove", (e) => {
      e.preventDefault();
    });

    // Initial state
    updateArrows();
  });
</script>